@model List<Cinema_Management_System.Models.Cinema.Movie>

<h2 class="mb-4">Aktualna lista filmów</h2>

@if (!Model.Any())
{
    <p>Brak filmów w bazie danych.</p>
}
else
{
    <table class="table table-bordered table-striped">
        <thead class="table-dark">
            <tr>
                <th>Tytu³</th>
                <th>Edytuj</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var movie in Model)
            {
                <tr>
                    <td>@movie.Title</td>
                    <td>
                        <a asp-controller="EditMovie" asp-action="EditOneMovie" asp-route-id="@movie.Id" class="btn btn-primary btn-sm">
                            Edytuj
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}