@model List<Cinema_Management_System.DTOs.Employee.DeleteMovieDTO>

<h2>Usuñ filmy</h2>


<form asp-action="DeleteMovie" asp-controller="DeleteMovie" method="post">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Wybierz</th>
                <th>Tytu³ filmu</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var movie in Model)
            {
                <tr>
                    <td>
                        <input type="checkbox" name="selectedMovieIds" value="@movie.Id" />
                    </td>
                    <td>@movie.Title</td>
                </tr>
            }
        </tbody>
    </table>
    <button type="submit" class="btn btn-danger">Usuñ wybrane</button>
</form>